import React from 'react'
import Courses from '../../components/Courses/Courses';
const CoursesPage = (currUser) => {
        return (
          <Courses currUser={currUser}/>
        )
      };

export default CoursesPage;
